export const defaultPasswordSalt = 'passwordSalt';
export const defaultHashedPassword = 'hashedPassword';

export const defaultSaltAndPassword = {
  passwordSalt: defaultPasswordSalt,
  hashedPassword: defaultHashedPassword,
};
